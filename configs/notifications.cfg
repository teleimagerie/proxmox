# Copie de /etc/pve/notifications.cfg (cluster, répliqué pmxcfs) — 30/08/2026.
# Le username réel est la clé API Mailjet (remplacée ici par un placeholder) ;
# le mot de passe (clé secrète) vit dans /etc/pve/priv/notifications.cfg,
# root-only et répliqué. Voir 17-zabbix.md § Supervision des sauvegardes.

matcher: default-matcher
	comment Route all notifications to mail-to-root
	disable true
	mode all
	target mail-to-root

smtp: mailjet
	comment Filet de secours : erreurs seulement (Zabbix est le canal principal)
	from-address pve@teleimagerie.net
	mailto mcapon@teleimagerie.net
	mode starttls
	port 587
	server in-v3.mailjet.com
	username CLE-API-MAILJET

sendmail: mail-to-root
	comment Send mails to root@pam's email address
	mailto-user root@pam

matcher: erreurs-mailjet
	comment warning+error+system-mail -> Mailjet ; les succes (info) sont avales
	match-severity warning,error,unknown
	target mailjet
